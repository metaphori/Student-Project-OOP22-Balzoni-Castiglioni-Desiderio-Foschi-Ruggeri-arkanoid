package it.unibo.game.app.model;

import it.unibo.game.Pair;
import it.unibo.game.app.api.GameObject;
import it.unibo.game.app.api.MovingObject;

public class Move {
    public void next(GameObject obj){ /*sarebbe meglio MovingObject */
        /*var newPos = Pair<Integer, Integer> (obj.getPos().getX() +obj.getDir().getX(), obj.getPos().getY() + obj.getDir.getY());
        obj.setPos(newPos);*/
    }
}
